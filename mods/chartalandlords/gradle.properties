# Sets default memory used for gradle commands. Can be overridden by user or command line properties.
org.gradle.jvmargs=-Xmx1G
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configuration-cache=true

#read more on this at https://github.com/neoforged/ModDevGradle?tab=readme-ov-file#better-minecraft-parameter-names--javadoc-parchment
# you can also find the latest versions at: https://parchmentmc.org/docs/getting-started
parchment_minecraft_version=1.21.1
parchment_mappings_version=2024.11.17
# Environment Properties
# You can find the latest versions here: https://projects.neoforged.net/neoforged/neoforge
# The Minecraft version must agree with the Neo version to get a valid artifact
minecraft_version=1.21.1
# The Minecraft version range can use any release version of Minecraft as bounds.
# Snapshots, pre-releases, and release candidates are not guaranteed to sort properly
# as they do not follow standard versioning conventions.
minecraft_version_range=[1.21.1]
# The Neo version must agree with the Minecraft version to get a valid artifact
neo_version=21.1.248
# The loader version range can only use the major version of FML as bounds
loader_version_range=[1,)

## Mod Properties

# The unique mod identifier for the mod. Must be lowercase in English locale. Must fit the regex [a-z][a-z0-9_]{1,63}
# Must match the String constant located in the main mod class annotated with @Mod.
#
# 命名规范（ChartaLandlords 系列）：
#   * **模组名** = 项目名 `ChartaLandlords`：mod_name / neoforge.mods.toml 的 displayName /
#     rootProject.name 都是它，玩家在模组列表里看到的就是这个名字。
#   * **产物名** = `charta-landlords`（mod_archive_name）：build/libs 与 dist 里的 jar 文件名，
#     按文件系统 / Maven 惯例用小写连字符。
#   * **mod_id** = 资源命名空间 = `chartalandlords`（与模组名同源）。本模组注册的一切都挂在它下面：
#     charta:game_type/chartalandlords:doudizhu、charta:rank/chartalandlords:small_joker、
#     chartalandlords:game_action、chartalandlords:card_select、
#     data/chartalandlords/**、assets/chartalandlords/**、语言键 chartalandlords.*。
#   * **组织命名空间** = Maven group / Java 包根，见 mod_group_id；Java 包用「组织 + 模块名」
#     （chartalandlords.doudizhu），不与 mod_id 绑死。
mod_id=chartalandlords
# The human-readable display name for the mod.
mod_name=ChartaLandlords
# 构建产物名（build/libs 与 dist 里的 jar）。
mod_archive_name=charta-landlords
# The license of the mod. Review your options at https://choosealicense.com/. All Rights Reserved is the default.
mod_license=MIT
# The mod version. See https://semver.org/
mod_version=1.8.0
# The group ID for the mod. It is only important when publishing as an artifact to a Maven repository.
# This should match the base package used for the mod sources.
# See https://maven.apache.org/guides/mini/guide-naming-conventions.html
mod_group_id=chartalandlords

# Charta (NeoForge 1.21.1) jar used for compilation and the dev runtime.
charta_version=1.2.5

## Development server port policy
# Every mod in this workspace runs its dev server on its own fixed port block so several
# `runServer` / `runClient` sessions can be up at the same time without clashing:
#   25580 block -> doudizhu   (server 25580, query 25580, rcon 25585)
# The values are materialised into run/server.properties by the `standardizeDevServer`
# Gradle task (and are written out here so they are discoverable without opening the task).
dev_server_port=25580
dev_query_port=25580
dev_rcon_port=25585
